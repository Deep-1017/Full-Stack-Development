function fetchDataFromDB(endpoint) {
    // Fetch the data from the database
}

function displayDataFromDB(response) {
    // Display the data from the database
}


let response = fetchDataFromDB('endpoint')
displayDataFromDB(response)